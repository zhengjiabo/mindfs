# AGENTS.md

This repo’s long-form agent notes live in [`AGENT.md`](./AGENT.md).

## Git push policy (hard rule)

- **Default push remote: `zhengjiabo` only.**
- **Do not** `git push origin ...`.
- **Do not** open pull requests against `origin/main` or `a9gent/mindfs`.
- Fetching/syncing from `origin` is allowed; writing is not.
- Only if the user **explicitly** asks for official/upstream (`origin`, `a9gent`, 官方, 上游) may you push to origin or open an upstream PR.
- User phrases like “push / 推送 / 合并到远程 / 同步远程” without naming official mean **`zhengjiabo`**.

See `AGENT.md` → **Git Remotes and Push Policy (MANDATORY)** for full detail.
